<?php
/**
 * LOCAL-ONLY credentials for testing on XAMPP.
 *
 * This file is listed in .gitignore, so it NEVER gets pushed to GitHub —
 * it only exists on your own computer. Railway will NOT use this file;
 * on Railway, you set SMTP_PASS directly in the "Variables" tab instead.
 *
 * Fill in your NEW Gmail App Password below (the one you generated after
 * revoking the old, exposed one).
 */
putenv('SMTP_USER=atisvincentcarl1@gmail.com');
putenv('SMTP_PASS=zdznqldgfqvvaqnx');